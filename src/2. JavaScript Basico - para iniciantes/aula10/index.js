// string, number, undefined, boolean, null
const nome = 'João'; // string
const nome1 = "João"; // string
const nome2 = `João`; // string
const num1 = 10; // number
const num2 = 3.14; // number
let nomeAluno; // undefined = não apont pra local nenhum na memoria 
const sobrenomeAluno = null; //nulo -> não aponta pra local nenhum na memoria
const aprovado = true; // boolean = true ou false (lógico)

let a =2;
const b = a;
console.log(a, b); //2, 2

a = 3;
console.log(a, b); //3, 2